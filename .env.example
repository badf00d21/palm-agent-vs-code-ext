# Mozaik Cloud (https://app.jigjoy.ai) — live loop view. Not an LLM key.
MOZAIK_API_KEY=
# MOZAIK_CLOUD_ENDPOINT=https://api.app.jigjoy.ai

# Provider keys for inference (Mozaik picks the SDK from the model name)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=not-needed
OPENAI_BASE_URL=http://localhost:11434/v1
LOCAL_MODEL=gemma4:12b
