# Cloud (optional, for heavy phases / comparison)
ANTHROPIC_API_KEY=

# Local via Ollama (Chat Completions)
OPENAI_API_KEY=not-needed
OPENAI_BASE_URL=http://localhost:11434/v1
# Ollama alias for qwen coder — Mozaik 3.x only accepts a closed ModelName set
# (create with: ollama cp qwen2.5-coder:14b deepseek-v4-pro)
LOCAL_MODEL=deepseek-v4-pro
